#include <iostream>

using namespace std;

int main()
{
    int v[100], n, i,nr =0;

    cin >>n;
    for(i = 1; i <= n; i++)
        cin >> v[i];

    for(i = 1; i <= n; i++){
      if(v[i] % 2 == 0 && i % 3 == 0)
          cout << v[i] << " ";
    }
    return 0;
}
