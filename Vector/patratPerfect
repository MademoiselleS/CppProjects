#include <iostream>
#include<math.h>

using namespace std;

int main()
{
    int v1[100], n;

    cin >> n;
    for(int i = 1; i <= n; i++){
        cin >> v1[i];
        if(sqrt(v1[i]) == (int)(sqrt(v1[i])))
            cout << v1[i] << " ";
    }

    return 0;
}
